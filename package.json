{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/dotenv": "^1.4.1",
    "nuxt": "^3.2.2"
  },
  "dependencies": {
    "@aws-amplify/auth": "^5.1.9"
  }
}
